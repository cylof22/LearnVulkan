# Project target
target=$(androidapilevel)
# Provide path to the directory where prebuilt external jar files are by setting jar.libs.dir=

#Provide path to the directory where prebuilt external .so files are by setting native.libs.absolute.dir=
